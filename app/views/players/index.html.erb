<br>
<small><%= link_to "Back to welcome screen", root_path %> </small>
<br>
<br>
<h3>&#x1F9B8;List of players</h3>
<br>

<% @players.each do |player| %>
	<small>
		(<%= player.id %>)
		<strong>#<%= player.shirtnumber %></strong>
	</small>
	<%= player.forename %>
	<%= player.name %>
	<small>
		(<%= player.team.club.shortname %>)
		<%= player.nationality %>
		 | 
		<%= player.position %>
	</small>
	<br>
<% end %>