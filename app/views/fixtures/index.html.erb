<br>
<small><%= link_to "Back to welcome screen", root_path %> </small>
<br>
<br>
<h3>&#x1F3DF;List of fixtures</h3>
<br>

<% @fixtures.each do |fixture| %>
	<small>(<%= fixture.id %>)</small>
	<strong>Stage <%= fixture.stage %> •</strong> 
	<%= fixture.hometeam.club.shortname %>
	 <strong>
	 	<%= fixture.scorehome %> - <%= fixture.scoreaway %>
	 </strong>
	 <%= fixture.awayteam.club.shortname %>
	 <small>(<%= fixture.season.competition.name %>)
	 @ <%= fixture.date %>
	 | <%= fixture.time %></small>
	<br>
<% end %>